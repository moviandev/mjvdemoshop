// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {apiKey: 'AIzaSyCTV7YmcG6lkS_viG7vlui3rFLrCDDWL0w',
  authDomain: 'mjvdemoshop.firebaseapp.com',
  databaseURL: 'https://mjvdemoshop.firebaseio.com',
  projectId: 'mjvdemoshop',
  storageBucket: 'mjvdemoshop.appspot.com',
  messagingSenderId: '699275119141',
  appId: '1:699275119141:web:d2a62adab031201a'}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
